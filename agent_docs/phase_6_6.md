# Phase 6.6 completion

Phase 6.6 closes additional audit gaps with a public periodic-prompt
configuration API/store, richer fallback chains, envelope-v2 compaction
markers, advanced loop events, and durable cost accounting.

The following remain deliberately out of coverage and are not claimed:
production Switchboard sidecar orchestration, summarization-model compaction,
durable cursor hook workers/auto-title, periodic firing scheduler, and OTLP
export. Their schema/seams are groundwork only until real Go + dark shadcn +
dark GPUI tests assert behavior.
