version: "2"
run:
  timeout: 5m
linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - unparam
issues:
  max-same-issues: 0
formatters:
  enable:
    - gofmt
    - goimports
