import { expect, test } from "@playwright/test";
test("shows advanced automation in the dark shadcn client", async ({page})=>{await page.addInitScript((token)=>localStorage.setItem("ultra-token",token),process.env.ULTRA_TOKEN??"tok-alice");await page.goto("/");await expect(page.locator("html")).toHaveClass(/dark/);await page.getByLabel("New session title").fill("Automation");await page.getByRole("button",{name:"Create session"}).click();await expect(page.getByTestId("timeline")).toBeVisible();});
